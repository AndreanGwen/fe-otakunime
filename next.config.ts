import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: true,
  images: {
    domains: ["otakudesu.best", "v1.samehadaku.how"],
  },
};

export default nextConfig;
